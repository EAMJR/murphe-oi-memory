# 2026-03-17 — Daily Log

## Evening Session: Heartbeat Issues & Git Fix

### Summary
User flagged that I've been "running into a lot of issues" lately. Investigation revealed:

1. **Heartbeat interval collapse** (GitHub issue #27807): Native heartbeat firing every 5-8 min instead of configured 90m
2. **Git repo path confusion**: Cron job was looking for repos in wrong locations (relative paths vs actual absolute paths)
3. **Excessive HEARTBEAT_OK responses**: Due to interval collapse, responding to heartbeats constantly

### Fixes Applied

#### Cron Job Path Corrections
Fixed `murphe:daily-consolidated-sync` job with absolute paths:
- `/home/node/.openclaw/memory/` → murphe-oi-memory.git
- `/home/node/.openclaw/workspace/` → openclaw-core.git  
- `/home/node/.openclai/workspace/projects/murphe-oi/` → murphe-oi.git

Also fixed `murphe:weekly-memory-review` to read from correct memory path.

### Technical Notes
- Git repos verified at correct locations
- All remotes configured with embedded tokens (ghp_*)
- Next sync: Tomorrow 4 AM EST

### Remaining Issue
The native heartbeat system itself still has the interval collapse bug. Workaround is the existing cron jobs, but root cause is upstream in OpenClaw issue #27807.

---
*Session: Pre-compaction flush due to heartbeat spam accumulated context*
