# Healthcheck Report - 2026-02-04

## System Context
- **OS**: Debian GNU/Linux 12 (Bookworm)
- **Environment**: Docker container on Hostinger VPS
- **OpenClaw Version**: 2026.2.2
- **Gateway Bind**: LAN (0.0.0.0:62443)
- **Runtime**: Container as user `node` (non-root)

## Issues Found & Fixed

### ✅ FIXED: Insecure Auth
- **Issue**: `controlUi.allowInsecureAuth: true` on public-facing server
- **Fix**: Changed to `false` in openclaw.json
- **Impact**: Web UI now requires HTTPS or local access
- **Your Token**: `5OcjSVJ7XNSZznWqCRGO5uGo6s66K1IP` (save this!)

### ⏳ PENDING: Host Auto-Updates
- **Issue**: `unattended-upgrades` not installed in container
- **Action Required**: Install on host with:
  ```bash
  sudo apt-get install -y unattended-upgrades
  sudo dpkg-reconfigure -plow unattended-upgrades
  ```

### ✅ FIXED: No Backups
- **Solution**: Automated backup system via OpenClaw cron

## Backup System Created

### 1. Daily Memory Backup
- **Schedule**: Every day at 03:00 UTC
- **Job**: `healthcheck:memory-backup`
- **Target**: `/home/node/.openclaw/workspace/memory/`
- **Method**: Git commits (ready for remote push)

### 2. Weekly Config Backup
- **Schedule**: Every Sunday at 03:30 UTC
- **Job**: `healthcheck:config-backup`
- **Target**: `~/.openclaw/` tarball
- **Location**: `/home/node/.openclaw/backups/`
- **Retention**: Last 4 backups kept

## Cron Jobs Active
```
healthcheck:memory-backup  | daily @ 03:00 UTC
healthcheck:config-backup  | weekly @ 03:30 UTC (Sundays)
```

## Next Steps for Remote Backup
To push memory to GitHub/GitLab:
1. Create a private repo
2. Add remote: `cd /home/node/.openclaw/workspace/memory && git remote add origin <repo-url>`
3. The daily cron will auto-push if remote is configured

## Security Notes
- Config file permissions: 600 (correct)
- OpenClaw directory: 700 (correct)
- Secrets stored in plaintext in config (standard, protected by permissions)
- Running as non-root user (good)

---
Generated by OpenClaw healthcheck skill
